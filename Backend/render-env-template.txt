# Render Environment Variables Template
# Copy these variables to your Render Dashboard Environment Variables section

# Required Variables
NODE_ENV=production
PORT=10000
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/badminton-booking
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Optional Variables (uncomment and set as needed)
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password
# GOOGLE_MAPS_API_KEY=your-google-maps-api-key
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
# MAX_FILE_SIZE=5242880
# ALLOWED_FILE_TYPES=image/jpeg,image/png,image/jpg,application/pdf

# Notes:
# 1. Replace 'username:password@cluster.mongodb.net' with your actual MongoDB connection string
# 2. Generate a strong JWT_SECRET (you can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
# 3. Set up MongoDB Atlas and whitelist Render's IP addresses (0.0.0.0/0 for testing, specific IPs for production)
# 4. Test your MongoDB connection string locally before deploying 